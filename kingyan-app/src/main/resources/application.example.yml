spring:
  # 国际化配置
  messages:
    basename: i18n/message
  # 选择环境
  profiles:
    active: debug

sm2-key-pair:
  private-key-hex:
  public-key-hex:

---
debug: false
spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/kingyan?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

http-header:
  # 本地调试时，允许浏览器跨域访问。部署时应当用nginx代理，前后端同源
  access-control-allow-origin: http://localhost:8085

config:
    activate:
      on-profile: debug
---
server:
  port: 80
spring:
  config:
    activate:
      on-profile: prd